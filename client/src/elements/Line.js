import styled from 'styled-components';

export const Line = styled.div`
    width: 35px;
    border-bottom: 4px solid ${props => props.theme.inputBG};
    border-radius: 5px;
    position: relative;
    margin: 0 auto;
    margin-bottom: 44px;
`